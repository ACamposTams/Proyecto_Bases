<%= form_for(@clientes_juego) do |f| %>
  <% if @clientes_juego.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clientes_juego.errors.count, "error") %> prohibited this clientes_juego from being saved:</h2>

      <ul>
      <% @clientes_juego.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_juego %><br>
    <%= f.text_field :id_juego %>
  </div>
  <div class="field">
    <%= f.label :id_cliente %><br>
    <%= f.text_field :id_cliente %>
  </div>
  <div class="field">
    <%= f.label :total %><br>
    <%= f.text_field :total %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
