<%= form_for(@tiendas_consola) do |f| %>
  <% if @tiendas_consola.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tiendas_consola.errors.count, "error") %> prohibited this tiendas_consola from being saved:</h2>

      <ul>
      <% @tiendas_consola.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cantidad %><br>
    <%= f.number_field :cantidad %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
